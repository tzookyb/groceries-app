// Shared by Header/TabBar/Panel so every row lines up and the app keeps a
// phone-like width instead of stretching edge-to-edge on desktop.
export const SHELL_WIDTH = 'w-[calc(100%-32px)] max-w-[480px] mx-auto';
